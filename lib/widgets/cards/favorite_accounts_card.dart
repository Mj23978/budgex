import 'package:flutter/material.dart';

class FavoriteAccountsCard extends StatelessWidget {
  const FavoriteAccountsCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
